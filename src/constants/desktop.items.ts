import { MenuItem } from "../types/menu-item.types";
import { DesktopItems } from "./DesktopItems";

export const desktopItems: MenuItem[] = [
  DesktopItems.MY_COMPUTER,
  DesktopItems.RECYCLE_BIN,
];
