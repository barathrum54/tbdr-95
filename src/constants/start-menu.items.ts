import { MenuItem } from "../types/menu-item.types";
import { StartMenuItems } from "./StartMenuItems";

export const menuItems: MenuItem[] = [
  StartMenuItems.PROGRAMS,
  StartMenuItems.DOCUMENTS,
  StartMenuItems.SETTINGS,
  StartMenuItems.FIND,
  StartMenuItems.HELP,
  StartMenuItems.RUN,
  StartMenuItems.SHUTDOWN,
];
